<nav class="navbar navbar-inverse navbar-embossed navbar-static-top" role="navigation">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-01">
      <span class="sr-only">Davakhana</span>
    </button>
    <a class="navbar-brand" href="#"> Mana Davakhana </a>
  </div>
  <div class="collapse navbar-collapse" id="navbar-collapse-01">
    <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <%= render "shared/patient_nav" %>
        <%= render "shared/appointment" %>
      </ul>
    <% end %>
    <div class="navbar-form navbar-right">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li>
            <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
          </li>
        <% else %>
          <li>
            <%= link_to('Login', new_user_session_path)  %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav><!-- /navbar -->
